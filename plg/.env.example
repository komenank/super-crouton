# ── Grafana ──────────────────────────────────────────
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme
GF_PORT=13000

# Public URL — used for OAuth redirects, links in alerts, etc.
# If you expose Grafana through Cloudflare Tunnels, set this to the public URL.
GF_ROOT_URL=https://logs.example.com
