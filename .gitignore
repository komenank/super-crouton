# Environment secrets
.env

# Certificates — keep the folder, ignore actual cert files
certs/*
!certs/.gitkeep

# RdpGw configuration
rdpgw/config/rdpgw.yaml

# Local database backups (do not commit dumps)
backups/*
!backups/.gitkeep
